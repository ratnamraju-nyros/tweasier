.content
  %h3= "Unfollowed people (via Tweasier)"

  %p Here are all the people that Tweasier has unfollowed for you. We limit the number of users shown to those unfollowed in the last 2 weeks. These are only people that are generated by the bulk follower tool, your other followers have not been touched.
  %p
    The bulk unfollower tool is
    %strong= @account.auto_unfollow? ? "active." : "inactive."
    = link_to "Click here", edit_app_user_account_path(@account)
    to change this.

  %p.statistic
    %strong= pluralize((@people_count), "person")
    \...unfollowed

  - unless @grouped_people.empty?
    %h4 Unfollowed people by day

    = flash_chart app_user_account_statistics_unfollowed_people_path(@account, :format => :chart), :height => 500
    %br.clear
    
    %table.fancy
      %tr
        %th Day
        %th Unfollowed count
      - @grouped_people.each do |day, people|
        %tr
          %td.highlight= day
          %td= pluralize(people.size, "person", "people")

  %h4 User detail
  = render_list(:unfollowed_people, @people.slice(0,200), "app/statistics")

= render :partial => "app/statistics/shared/sidebar"